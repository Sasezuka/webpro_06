<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>数あてゲーム</title>
</head>
<body>
    <h1>数あてゲーム</h1>
    <p><%= message %></p>
    <% if (guess !== null) { %>
        <p>あなたの推測: <%= guess %></p>
    <% } %>
    <p>試行回数: <%= attempts %></p>
    <form action="/guess" method="get">
        <label for="guess">1から100の数を入力してください:</label>
        <input type="number" id="guess" name="guess" min="1" max="100" required>
        <button type="submit">送信</button>
    </form>
</body>
</html>